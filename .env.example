# AI Translation API Keys
# Copy this file to .env and add your actual keys
# The .env file is gitignored and will not be committed
#
# Usage:
#   1. Copy this file: cp .env.example .env
#   2. Add your API keys below (remove the # and add your key)
#   3. Run translation script: npm run translation:ai -- --plugin=fair-events --locale=fr_FR --provider=openai --auto-write
#
# The translation scripts will automatically load keys from this file.
# No need to export environment variables manually!

# OpenAI API key for AI translations (GPT-4o-mini)
# Get your key from: https://platform.openai.com/api-keys
# Cost: $0.15/$0.60 per 1M tokens (input/output)
#OPENAI_API_KEY=sk-...

# Anthropic API key for Claude AI translations (Claude 3.5 Haiku)
# Get your key from: https://console.anthropic.com/settings/keys
# Cost: $0.80/$4.00 per 1M tokens (input/output)
#ANTHROPIC_API_KEY=sk-ant-...
