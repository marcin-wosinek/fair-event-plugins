---
"fair-audience": patch
---

The "Record email consent" popup now lists participants in the same order as the printable participant list, following the Audience table's active sort instead of the raw participant fetch order.
