---
"fair-events": minor
---

Replace the Signups tab's "Download CSV" button with a configurable "Export" popup: pick all columns or handpick them, choose Markdown/CSV/one-line output, and optionally include Fair Form answers collected during signup as individually selectable columns.
