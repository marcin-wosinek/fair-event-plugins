---
"fair-audience": minor
---

Add a `GET /participants/stats` endpoint and a row of clickable stat tiles (total, mailing, pending, declined) above the All Participants table, so audience size and mailing health are visible without manually combining filters (issue #1054).
