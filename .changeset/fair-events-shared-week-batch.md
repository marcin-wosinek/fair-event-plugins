---
"fair-events-shared": minor
---

Add a shared MiniCalendar primitive (extracted from the occurrences calendar) so the series-modal date picker and the Sale Periods panel render an identical shaded, click-to-pick grid, and add a renderPaymentError helper that shows sanitized payment gateway errors with an admin-only interpreted cause and fix-it links.
