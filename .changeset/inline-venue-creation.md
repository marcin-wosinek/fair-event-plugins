---
'fair-events': minor
---

Add inline venue creation to the calendar event form. The venue selector on the calendar's Quick Add Event modal and the Manage Event screen now ends with an "Add new venue" option that opens a simplified name/address form right below the selector, without leaving the event workflow. Creating a venue there selects it automatically, keeps the rest of the event form untouched, and reuses the existing venues REST endpoint.
