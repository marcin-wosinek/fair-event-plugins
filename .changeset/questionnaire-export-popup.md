---
"fair-form": patch
---

Replace the separate "Export CSV" and "Copy Markdown" buttons on the Questionnaire Responses admin page with a single "Export" button that opens a popup letting you choose columns (all or handpicked) and format (Markdown, CSV, or one line per person) before copying to clipboard or downloading the CSV.
