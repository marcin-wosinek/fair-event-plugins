---
"fair-audience": patch
---

Preserve scroll position when switching the recurrence occurrence picker on the event-signup block, which previously reset to the top of the page on the `?event_date=<id>` reload.
