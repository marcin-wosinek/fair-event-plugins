---
"fair-events-experimental": minor
---

Report both test-mode and live-mode checkouts/purchases to Meta Test Events (using the configured Test Events code), tagging each delivered event with its payment mode so administrators can distinguish test traffic from live sales in delivery diagnostics.
