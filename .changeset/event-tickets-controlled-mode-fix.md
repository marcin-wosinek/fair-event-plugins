---
"fair-events": patch
---

Fix the ticket editor's "Save tickets" button and JSON import misfiring against a nonexistent event when the component is used in controlled mode (e.g. embedded in another plugin's wizard) instead of Manage Event's own self-load/self-save mode.
