---
"fair-audience": patch
---

Fix stale `fair-audience-submission-detail` links in the participant timeline and participant detail page — the submission detail page moved to `fair-form`, so these now point at `fair-form-submission-detail`.
