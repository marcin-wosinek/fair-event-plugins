<?php
/**
 * Render callback for the Fair Form Option block.
 *
 * This block does not render standalone — its parent block
 * (select-one or multiselect) iterates over inner blocks
 * to build the appropriate HTML.
 *
 * @package FairAudience
 * @param array    $attributes Block attributes.
 * @param string   $content    Block content.
 * @param WP_Block $block      Block instance.
 * @return string Empty string — rendered by parent.
 */

defined( 'WPINC' ) || die;

return '';
